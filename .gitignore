*.exe
*_best.txt
baseline_*.txt
after_*.txt
regen_*.txt
.regression_tmp/
.baseline/
build/
talib/
python/__pycache__/

# Secrets — the real .env must never be committed.
.env
.env.*

# ...but the placeholder template must stay tracked.
!.env.example
